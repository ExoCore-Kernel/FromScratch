import {startQemu64,stopQemu64} from './browser-qemu64.js';const imageInput=document.querySelector('#imageInput'),memorySelect=document.querySelector('#memorySelect'),startButton=document.querySelector('#startButton'),stopButton=document.querySelector('#stopButton'),terminal=document.querySelector('#terminal'),status=document.querySelector('#status');function setStatus(message,isError=false){status.textContent=message;status.dataset.error=isError?'true':'false';}startButton.addEventListener('click',async()=>{startButton.disabled=true;try{await startQemu64({file:imageInput.files?.[0],terminal,memoryMb:Number(memorySelect.value),onStatus:setStatus});}catch(error){console.error(error);setStatus(error instanceof Error?error.message:String(error),true);}finally{startButton.disabled=false;}});stopButton.addEventListener('click',async()=>{await stopQemu64();terminal.replaceChildren();setStatus('Virtual machine stopped.');});window.addEventListener('beforeunload',()=>stopQemu64());
